/// A test data class representing a node in a linked list.
///
/// Each node contains an optional integer value and a reference to the next
/// node.
class Node {
  final int? value;
  final Node? next;

  Node({this.value, this.next});
}
